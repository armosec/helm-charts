name: pr-allowlist-drift
# Runs the GKE Autopilot allowlist drift check (both ARMO and Rapid7 node-agents) on every PR
# that touches a node-agent render, a vendored allowlist, or the drift tooling. Unlike the
# release gate (a manual BYPASS_ALLOWLIST_DRIFT input), a PR is automated — so on drift this
# notifies the PR author with a sticky comment and fails the check until a maintainer adds the
# `allowlist-drift-ack` label (the PR-flow equivalent of "set as pass after you opened Gerrit").
on:
  pull_request:
    types: [opened, reopened, synchronize, ready_for_review, labeled, unlabeled]
    paths:
      - 'charts/kubescape-operator/**'
      - 'charts/rapid7-operator/**'
      - 'gke-allowlist/**'
      - 'scripts/gke-allowlist/**'

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: write   # post/update the sticky drift comment

jobs:
  allowlist-drift:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Install Helm
        uses: azure/setup-helm@v3.5

      - name: Install Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - run: pip install pyyaml

      - name: Check node-agents vs approved allowlists
        id: drift
        run: |
          drift=0; crash=0; OUT=""
          check() {
            set +e
            local out; out=$(scripts/gke-allowlist/check-drift.sh "$@" 2>&1); local rc=$?
            set -e
            OUT+=$'\n### '"$2"$'\n'"$out"$'\n'
            # exit 2 = spec-drift (ack-able); 0 = OK; anything else = the check crashed.
            if [ "$rc" -eq 2 ]; then drift=1; elif [ "$rc" -ne 0 ]; then crash=1; fi
          }
          check charts/kubescape-operator gke-allowlist/armo-private-node-agent-1.40-v2.yaml --wrapper
          check charts/rapid7-operator   gke-allowlist/armo-rapid7-node-agent-1.40-v2.yaml  --wrapper
          if [ "$crash" -ne 0 ]; then rc=3; elif [ "$drift" -ne 0 ]; then rc=2; else rc=0; fi
          echo "$OUT"
          echo "rc=$rc" >> "$GITHUB_OUTPUT"
          {
            echo "report<<DRIFT_EOF"
            echo "$OUT"
            echo "DRIFT_EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Notify author & conclude
        uses: actions/github-script@v7
        env:
          RC: ${{ steps.drift.outputs.rc }}
          REPORT: ${{ steps.drift.outputs.report }}
          ACK_LABEL: allowlist-drift-ack
        with:
          script: |
            const marker = '<!-- allowlist-drift -->';
            const rc = process.env.RC;
            const ackLabel = process.env.ACK_LABEL;
            const { owner, repo } = context.repo;
            const pr = context.payload.pull_request;
            const issue_number = pr.number;
            const author = pr.user.login;
            const acked = (pr.labels || []).map(l => l.name).includes(ackLabel);

            async function findSticky() {
              const cs = await github.paginate(github.rest.issues.listComments, { owner, repo, issue_number });
              return cs.find(c => c.body && c.body.includes(marker));
            }
            async function upsert(body) {
              body = marker + '\n' + body;
              const existing = await findSticky();
              if (existing) await github.rest.issues.updateComment({ owner, repo, comment_id: existing.id, body });
              else await github.rest.issues.createComment({ owner, repo, issue_number, body });
            }
            async function clear() {
              const existing = await findSticky();
              if (existing) await github.rest.issues.deleteComment({ owner, repo, comment_id: existing.id });
            }

            if (rc === '0') {
              await clear();
              core.info('Both node-agents match their approved allowlists — no drift.');
              return;
            }

            if (rc !== '2') {
              await upsert('🛑 The GKE Autopilot allowlist drift check **failed to run** (exit `' + rc + '`). '
                + 'This is not a drift result, so the `' + ackLabel + '` label does **not** bypass it — please fix the check.');
              core.setFailed('allowlist drift check crashed (exit ' + rc + ')');
              return;
            }

            // rc === '2' : spec drift in one or both charts
            const detail = '<details><summary>Drift detail (ARMO + Rapid7)</summary>\n\n```\n'
              + (process.env.REPORT || '').trim() + '\n```\n</details>';
            const body = [
              '⚠️ **@' + author + '** — this PR\'s node-agent render **drifts from an approved GKE Autopilot allowlist** (`armo-private-node-agent` / `armo-rapid7-node-agent`).',
              '',
              'On GKE Autopilot the affected node-agent would be rejected by GKE Warden until Google approves a matching allowlist, so customers could not install this chart version.',
              '',
              '**To unblock this PR:**',
              '1. Open a Gerrit allowlist update at `gke-ap-allowlist.googlesource.com/ARMO` (see `scripts/gke-allowlist/README.md`), then update the vendored `gke-allowlist/` copy once approved.',
              '2. A maintainer adds the **`' + ackLabel + '`** label to this PR to acknowledge the drift and pass the check.',
              '',
              detail,
            ].join('\n');
            await upsert(body);

            if (acked) {
              core.notice('Drift acknowledged via the `' + ackLabel + '` label — check passes.');
            } else {
              core.setFailed('node-agent drifts from an approved allowlist. Open a Gerrit update, then add the `' + ackLabel + '` label.');
            }
